# Ejemplo de variables de entorno para linear-algebra-django
# Copia este archivo como .env y ajusta valores. Nunca subas tu SECRET_KEY real.

# Clave secreta de Django (genera una con: python -c "from django.core.management.utils import get_random_secret_key as g; print(g())")
SECRET_KEY=REPLACE_ME_WITH_SECURE_KEY

# Debug (True solo en desarrollo)
DEBUG=False

# Hosts permitidos (separados por coma)
ALLOWED_HOSTS=yourdomain.com

# Base de datos (ejemplos)
# SQLite local:
# DATABASE_URL=sqlite:///db.sqlite3
# PostgreSQL:
# DATABASE_URL=postgres://user:password@host:5432/dbname
DATABASE_URL=sqlite:///db.sqlite3
